require "spec_helper.rb"

describe Huffman::Node do 

	xit "can get a node and it will be inserted depending on the way we want to" do
		
		a = Node.new([1,"a"])

		b = Node.new([50,'b'])
		b = Node.new([50,'b'])

	end
end